// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí va tu configuración actual
};
// Añade esta línea para cambiar el puerto
nextConfig.serverOptions = {
  port: 3030, // O cualquier otro puerto que prefieras
};

export default nextConfig;
